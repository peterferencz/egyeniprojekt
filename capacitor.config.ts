import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.peterferencz',
  appName: 'egyeniprojekt',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
